-module(jsonifier).
% -export([
%     init/3,
%     data/4,
%     info/3,
%     terminate/3,
%     early_error/5
% ]).

% init(StreamID, Req, Opts) ->


% data(StreamID, IsFin, Data, State)

% info(StreamID, Info, State)

% terminate(_StreamID, _Reason, _State) ->
%     ok.

% early_error(_StreamID, _Reason, _PartialReq, _Resp, _Opts) ->
%     {response, 500, #{}, <<"internal server error">>}.